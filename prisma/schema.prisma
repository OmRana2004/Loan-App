generator client {
  provider = "prisma-client-js"
  engineType = "library"
}

datasource db {
  provider = "postgresql"
}

model Enquiry {
  id        String   @id @default(uuid())
  name      String
  phone     String
  loanType  String
  message   String?
  createdAt DateTime @default(now())
}
